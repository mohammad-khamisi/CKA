apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: longhorn-with-snapshot
provisioner: driver.longhorn.io
allowVolumeExpansion: true
parameters:
  numberOfReplicas: "2"
  recurringJobSelector: '[{"name":"test-group","isGroup":true}]'
